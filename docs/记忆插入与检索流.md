# Memory Skill — 插入与检索流

> 版本: V8 (Weaver MemPalace/Mem0 事实列表格式)
> 更新时间: 2026-06-07

---

## 一、架构总览

```
                    ┌─────────────────────────────────┐
                    │         Room Agent              │
                    │  (Agent 不感知 Memory 存在)      │
                    └──────┬──────────────┬───────────┘
                           │              │
                    ② 注入 │              │ ① 存储（发言后）
                    (读链) │              │ (写链)
                           ▼              ▼
              ┌─────────────────────────────────────┐
              │      MemorySkillAdapter             │
              │  实现 MemoryProtocol, 桥接 Room      │
              └──────────────┬──────────────────────┘
                             │
              ┌──────────────▼──────────────────────┐
              │          MemorySkill                │
              │        (组合层, 薄委托)              │
              └───┬───┬───┬───┬───┬───┬───┬────────┘
                  │   │   │   │   │   │   │
          ┌───────┼───┼───┼───┼───┼───┼───┼───────┐
          │ Weaver│Ret│Emb│DS │LS │Evo│Feed│       │
          │ (编织)│   │   │   │   │   │   │       │
          └───────┴───┴───┴───┴───┴───┴───┴───────┘
```

**插件**: Memory 不知 Persona/Emotion/TTS 的存在。桥接在 `RoomAssembly` 层。

---

## 二、写链 — Agent 发言 → 持久化

### 2.1 入口

```
Room.ConversationLoop
  └─ await agent.memory.add(msg, partner=partner)
```

每次 Agent 或 User 发言后，`ConversationLoop` 调用 `MemorySkillAdapter.add()`。

### 2.2 Adapter.add() [room_adapter.py:106-131]

```python
role = "assistant" if message.role.value in ("agent", "assistant") else "user"
```

- 使用 `Message.role` 枚举（非 `sender` 字符串比较）判定角色
- `display_name` 控制 Weaver 显示标签（如 `Agent:`），`agent_name` 控制 namespace（如 `agent_a`）
- 自动维护：每 10 轮触发 `consolidate()`, 每 50 轮触发 `clean()`

### 2.3 MemorySkill.ingest() [skill.py:151-189]

```python
effective_ns = self._ns_for(turn.partner)  # agent_name or "default"
self._ingestor.ingest_dialogue(turn, category=effective_ns)
```

### 2.4 Ingestor.ingest_dialogue() [ingestor.py:227-281]

```
DialogueTurn
  │
  ├─ ① SawRingBuffer.put()          ← 始终存储（环形缓冲区, 容量 1000）
  │
  ├─ ② ImportanceScorer.evaluate()   ← 重要性门控
  │     规则: 关键词("记住"/"重要") +0.3, 偏好词("我喜欢") +0.2
  │     阈值: 0.3 → 低于则跳过（仅 SawRingBuffer）
  │
  ├─ ③ DialogueStore.insert(turn)    ← SQLite 持久化
  │     ├─ dialogue_turns 表: INSERT OR IGNORE
  │     └─ dialogue_fts 表(FTS5): _cn_tokenize(content)
  │         ┌─────────────────────────────────────────┐
  │         │ jieba 分词: "我最拿手的是玉子烧哦"        │
  │         │   → "我 最 拿手 的 是 玉子烧 哦"        │
  │         │   → FTS5 unicode61 索引                 │
  │         │ 定制词典: jieba_dict.txt                │
  │         │   (玉子烧 蛋包饭 天妇罗 章鱼烧 炸猪排...) │
  │         └─────────────────────────────────────────┘
  │
  ├─ ④ _extract_entities(content)    ← 正则提取专名 (英文大写词)
  │
  └─ ⑤ LearnedStore.insert(entry)    ← ChromaDB 向量存储
       ├─ Embedder.embed(content) → ONNX bge-large-en-v1.5 → [1024] float
       ├─ MemoryEntry(id, content, weight=0.5, category=ns, tags, metadata)
       └─ ChromaDB collection.add() (HNSW + cosine)
```

### 2.5 DialogueStore 数据模型 [dialogue_store.py]

```sql
-- dialogue_turns 表
CREATE TABLE dialogue_turns (
    id       TEXT PRIMARY KEY,
    role     TEXT NOT NULL,       -- "user" | "assistant"
    content  TEXT NOT NULL,
    timestamp REAL NOT NULL,
    saw_index INTEGER,
    UNIQUE(role, content, timestamp)
);

-- FTS5 全文索引表 (jieba 分词后写入)
CREATE VIRTUAL TABLE dialogue_fts USING fts5(
    turn_id, content,
    tokenize = 'unicode61'
);
```

- `search(query, limit)`: FTS5 MATCH + BM25 排序
- `get_recent(n)`: 最近 n 条 (ORDER BY timestamp DESC + reverse)
- `count()`: 全部对话数
- `count_recent(minutes)`: 最近 N 分钟内的对话数（用于会话感知深度门控）
- `reindex_roles()`: 迁移工具 — 将 [chat] 多消息块拆分为独立 turn

---

## 三、读链 — 用户消息到达 → 记忆注入 Agent Prompt

### 3.1 入口

```
Room.agent._step()
  ├─ memory_ctx = await self.memory.weave(user_msg)
  └─ msgs = self._template.render(agent, history, memory_ctx)
```

### 3.2 Adapter.weave() [room_adapter.py:148-200]

```python
ctx = self._skill.weave(user_message=message, partner=p, scene_summary=scene_summary)
block = ctx.to_prompt_block()

# display_name 替换: agent_name(agent_a) → display_name(Agent)
if self.display_name != self.agent_name:
    block = block.replace(f"{self.agent_name}: ", f"{self.display_name}: ")
    block = block.replace(f"[{self.agent_name}说的]", f"[{self.display_name}说的]")

# 截断: 默认 3000 chars
if len(block) > self._max_context_chars:
    block = block[:self._max_context_chars].rsplit("\n", 1)[0] + "\n..."
```

### 3.3 Weaver.weave() — 四层编织 [weaver.py:88-133]

```
weave(skill, user_message, scene_summary, partner)
  │
  ├─ 深度门控:
  │   compact:  recent(5min) < 3 AND total_stored == 0 → 仅 tier1
  │   standard: recent(5min) ≤10 OR has stored data → tier1+tier2+emotion+nudge
  │   deep:     recent(5min) > 10 → 以上 + heartbeat
  │
  ├─ tier1 = _build_tier1(scene_summary)
  │   ├─ [当前场景] {scene_summary} | [当前感知] SawBuffer[-1]
  │   └─ [最近对话] get_recent(3) — 各条截断到 80 字符, 总 ≤420 字符
  │
  ├─ tier2 = _build_tier2(user_message, ns)      ← 标准/深度
  │   └─ Retriever.retrieve() → 3 信号 RRF 融合
  │
  ├─ emotion = _build_emotion_context(partner)
  │   └─ Evolution._outcomes[-50:] → 正/负比例 → 好感度提示 (≥3 样本)
  │
  └─ nudge = _build_nudge()
      └─ learned_store.search(weight≥0.85) → ⚠/💡 行为提示 (top 3)
```

#### Tier2 输出格式 (V8 MemPalace/Mem0)

```
以下是你知道的事实。你只能使用这些信息，不得编造:
  - [Agent说的] (fact text with source attribution)
  - [User说的] (user's own words)
（如果你不确定某条信息是否在上面列出——就不要提及它。）
```

**反幻觉约束**:
1. 每条事实标记 `[source说的]` — source 由 `role` 推断：
   - `role="assistant"` → `[{agent_name}说的]`
   - `role="user"` → `[User说的]`
2. 自引用跳过：跳过与当前用户消息前 25 字符相同的事实
3. 去重：同一事实只出现一次（前 60 字符 key）
4. 显式约束："你只能使用这些信息，不得编造"

### 3.4 Retriever.retrieve() — 3 信号 RRF 融合 [retriever.py:141-291]

```
retrieve(query, limit=5, filters={category: "agent_a"})
  │
  ├─ ① SEMANTIC: learned_store.search(query, limit=50, filters)
  │     ChromaDB HNSW cosine 相似度 → [MemoryEntry, ...]
  │     权重: rrf_semantic_weight = 0.5
  │
  ├─ ② BM25: dialogue_store.search(query[:80], limit=50)
  │     ┌──────────────────────────────────────────────┐
│     │ query: "User今天有做玉子烧吗"[:80]           │
│     │   → jieba.cut → "User 今天 有 做              │
  │     │      玉子烧 吗"                              │
  │     │   → FTS5 MATCH (unicode61 AND 语义)          │
  │     │   → BM25 排序                                │
  │     │ 权重: rrf_bm25_weight = 2.5  ← 主信号         │
  │     └──────────────────────────────────────────────┘
  │
  ├─ ③ TEMPORAL: _time_decay(entry, now)
  │     exp(-age_hours / 24.0)
  │     权重: rrf_temporal_weight = 0.5
  │
  └─ RRF 融合 (k=60):
      rrf_score = 0.5/(60+sem_rank) + 2.5/(60+bm25_rank) + 0.5/(60+temp_rank)
      final_score = rrf_score × (0.5 + entry.weight)        ← Evolution 权重
      final_score ×= (1.0 + 0.1 × entity_overlap)           ← 实体共现 (max 1.5x)
      → 排序 → Top-N → MemoryEnvelope
```

**为什么 BM25=2.5 是主信号**: 中文嵌入质量依赖训练数据。在 NSFW 内容上向量相似度不稳定，exact word match (BM25) 更可靠。语义作为兜底。

### 3.5 PromptTemplate.render() — 消息组装 [prompt.py:86-161]

修复前：记忆注入 `system` 消息 → **DeepSeek 角色扮演无视 system 指令**

修复后：记忆注入 `user` 消息 → DeepSeek 服从 user 消息

```
最终消息列表:
[0] system   ── ### 你是谁 / ### 你在哪里 / ### 行为规则
[1] user     ── [系统提醒] 以下是你的记忆记录。你只能使用这些信息，不得编造：
                  [当前场景] ...
                  [最近对话] Agent: ... | User: ...
                  以下是你知道的事实...
                  [你当前对user的感受] ...
[2] user     ── User: (历史用户消息 1)
[3] asst     ── Agent: (历史 Agent 回复 1)
[4] user     ── User: (当前用户消息)
[5] user     ── (Agent, 直接回应当前消息)
```

---

## 四、反馈链 — Agent 回复后 → 评估 → 权重演化

### 4.1 入口

```
Room.ConversationLoop
  └─ (Agent 回复后) memory.feedback_after_response(final_response)
```

### 4.2 Adapter.feedback_after_response() [room_adapter.py:197-243]

```python
outcome = auto_detect_outcome(query, search_results, final_response)
skill.record_outcome(mem_ids, outcome)
skill.evolve()
```

### 4.3 auto_detect_outcome() [feedback.py:64-107]

两条路径，LLM 优先：

| 路径 | 机制 | 激活条件 |
|------|------|----------|
| **LLM** | MiniCPM-5 1B GGUF 分类 → positive/negative/neutral | 默认启用，失败降级 |
| **Rule** | keyword overlap: 响应词 ∩ 检索结果词 > 30% → positive, 0% → negative | LLM 路径失败时 |

### 4.4 EvolutionLoop.tick() [evolution.py:145-227]

```
每轮演化 (tick):
  │
  ├─ 收集最后 100 条 outcomes
  ├─ 计算 per-memory deltas:
  │   positive → +0.1, negative → -0.2, neutral → 0
  ├─ 单轮 cap: max_delta = 0.15
  ├─ 边界约束: weight ∈ [0.05, 0.95]
  ├─ learned_store.set_weight(mem_id, new_weight)
  └─ 清除已处理 outcomes → 保存到 memory_outcomes.json
```

**演化周期**: 每 5 条 outcomes 触发一次, 最小 5 条样本, 跨 session 持久化。

---

## 五、配置参数

### 5.1 MemorySkillConfig [contracts.py:253-311]

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `rrf_bm25_weight` | 2.5 | BM25 信号权重 (主信号) |
| `rrf_semantic_weight` | 0.5 | 语义向量权重 |
| `rrf_temporal_weight` | 0.5 | 时间衰减权重 |
| `rrf_k` | 60 | RRF 融合常数 |
| `temporal_boost_hours` | 24.0 | 时间衰减半衰期 (小时) |
| `evolution_max_delta` | 0.15 | 单轮最大权重调整 |
| `evolution_min_samples` | 5 | 最小演化样本数 |
| `evolution_interval_seconds` | 300 | 演化间隔 |
| `embedding_dim` | 1024 | 嵌入向量维度 |
| `max_learned_entries` | 100,000 | ChromaDB 最大条目数 |

### 5.2 MemorySkillAdapter [room_adapter.py:52-64]

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `agent_name` | `""` | namespace key (ChromaDB category, DB 已存 "agent_a"/"user") |
| `display_name` | `""` | Weaver 显示标签 (如 "Agent") |
| `partner` | `"user"` | 默认对话对象 |
| `max_context_chars` | 3000 | 记忆上下文截断阈值 (约 750 tokens) |

### 5.3 Weaver 常量 [weaver.py:76-83]

| 常量 | 值 | 说明 |
|------|-----|------|
| `_TIER1_MAX_CHARS` | 320 | tier1 场景+对话上限 |
| `_TIER2_MAX_CHARS` | 1200 | tier2 单条事实上限 |
| `_NUDGE_MAX_CHARS` | 120 | nudge 单条上限 |
| `_MAX_RECENT_TURNS` | 3 | 最近对话条数 |
| `_NUDGE_WEIGHT_THRESHOLD` | 0.85 | nudge 最低权重 |
| `_NUDGE_CRITICAL_THRESHOLD` | 0.95 | ⚠ 强制提醒权重 |

---

## 六、本次会话修复清单 (2026-06-07)

| # | 问题 | 修复 | 文件 |
|---|------|------|------|
| 1 | `_count_recent_turns` == `_count_all_dialogue` — 新会话误判 deep | 新增 `count_recent(5min)`, 区分当前会话 vs 全部历史 | `dialogue_store.py`, `weaver.py` |
| 2 | 玉子烧/蛋包饭 被 jieba 切碎 | 新增 `jieba_dict.txt` 定制词典, `_get_jieba()` 自动加载 | `dialogue_store.py`, `jieba_dict.txt` |
| 3 | `_CREATE_TABLES_SQL` 易被 edit 损坏 | 添加 ⚠ CRITICAL 警告注释 | `dialogue_store.py` |
| 4 | Adapter role 检测失败 (sender 中文 ≠ agent_name 英文) | 改用 `Message.role` 枚举 | `room_adapter.py` |
| 5 | 现有 89 条数据全标为 `role="user"` | `reindex_roles()` 拆分 [chat] 格式 + 迁移 | `dialogue_store.py` |
| 6 | Weaver 显示 "助手" 而非 Agent 名 | `_build_tier1` 使用 `agent_name`, adapter 做 `display_name` 替换 | `weaver.py`, `room_adapter.py` |
| 7 | 记忆注入 system → DeepSeek 无视 | 记忆移入 `role="user"` 消息, `[系统提醒]` 前缀 | `prompt.py` |
| 8 | adapter `max_context_chars=800` 截断 tier2 | 提升至 3000 | `room_adapter.py` |
| 9 | ChromaDB category 不匹配 (查 "Agent", 存 "agent_a") | `agent_name`(ns key) ≠ `display_name`(label) | `room_adapter.py` |
| 10 | tier2 source 错标 "[User说的]" (实为Agent) | source 推断从 `partner` 改为 `role` | `weaver.py` |
