# Ablation Report — Memory Skill V2

**Generated:** 2026-06-01T13:35:27.736272+00:00
**DB:** C:\Users\PC\.local\share\opencode\opencode.db
**RRF weights:** semantic=1.0, BM25=2.0, temporal=0.5, k=60

---

## Methodology

- Dataset: **191** messages from OpenCode DB ingested identically for all 3 modes
- Test queries: **10** queries derived from ingested messages (first-sentence self-retrieval)
- Each mode uses a fresh MemorySkill instance with separate temp DBs
- Mode A uses only ``LearnedStore.search()`` (ChromaDB vector)
- Mode B uses only ``DialogueStore.search()`` (SQLite FTS5 BM25)
- Mode C uses ``Retriever.retrieve()`` (full 3-signal RRF fusion)

## Results

| Mode | Signals | Hits@10 | Recall@10 |
|------|---------|---------|-----------|
| A    | Vector only (ChromaDB semantic) | 9/10 | 90.0% |
| B    | BM25 only (SQLite FTS5) | 10/10 | 100.0% |
| C    | Full fusion (Vector + BM25 + Temporal) | 10/10 | 100.0% |

## Analysis

### 三因分离结论

1. ****BM25 关键词匹配** 是主导信号（100.00% vs 90.00%）。语义模型在当前数据上表现不足，可能是模型不匹配或文本域差异导致。**

2. **融合有效性** — Fusion (100.00%) ≈ 最佳单信号 BM25 (100.00%) → RRF 未提升也未降低，单信号已足够

3. **关键发现**:
   - RRF 权重当前为 semantic=1.0, BM25=2.0, temporal=0.5, k=60
   - 查询来自消息自身内容（self-retrieval），BM25 天然有优势（精确字符串匹配）
   - 真实的 query expansion 或 paraphrase 场景下召回会更低

### 瓶颈诊断

- **Primary bottleneck:** 向量和 BM25 表现接近 → self-retrieval 场景区分度不足（注意：查询来自消息自身内容）
- **Recommended action:** 两者均好 → 直接进入 Task 3（batch ingest 性能优化）

## Conclusion

| Question | Answer |
|----------|--------|
| Is RRF fusion better than single-signal? | No — single signal sufficient or weights need calibration |
| Which signal dominates? | BM25 (100.0%) |
| Does temporal signal help? | Not measurable in self-retrieval (same-timestamp source matches itself) |
| Next step | 两者均好 → 直接进入 Task 3（batch ingest 性能优化） |

---

*Report generated by bench_ablation.py*
