# 2026-07-19 Refactor

## Deleted (3 modules)
- `decay.py` — replaced by per-retrieval temporal decay
- `evolution.py` — complex tick/outcome loop replaced by simple decay+boost
- `rewriter.py` — dead code, no callers

## Architecture Changes
1. **Time injection** (weaver): `[现在]` + per-memory `[MM-DD HH:MM]`
2. **Temporal decay** (retriever): `weight × exp(-0.01 × hours)`, uses `updated_at`
3. **Semantic dedup** (learned_store + ingestor): `find_duplicate()` cosine>0.85 → merge
4. **Weight system**: init=0.5, dedup hit+0.05, weave cite+0.02
5. **No gate**: all content → both stores, semantic+time+boost filters naturally
6. **Weaver decoupled**: `WeaverStores` replaces `MemorySkill._*` access

## State
- 20 modules, ~5000 lines (was 22/5700)
- Verified: everyday content retrievable, semantics overrides weight gap
